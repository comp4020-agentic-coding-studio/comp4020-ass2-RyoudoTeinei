---
import CoursePage from "./CoursePage.astro";
const meta = Astro.props.frontmatter ?? Astro.props;
---
<CoursePage title={String(meta.title ?? "How to Leave")} description={meta.description} eyebrow="Course handbook">
  <slot />
</CoursePage>
